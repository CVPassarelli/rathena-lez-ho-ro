# Validator fixtures

These files exercise `tools/custom-validation/validate.py`. They are not under `db/`, `conf/`, or `npc/`, and no server loader references them. Invalid fixtures must never be copied into runtime import directories.
